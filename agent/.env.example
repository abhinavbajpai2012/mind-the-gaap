# Copy to .env and fill in. .env is gitignored; never commit real tokens.
#
#   cp .env.example .env
#
# TabPFN hosted-inference token. Each team member should register their own
# rather than sharing one, so the account rate limit is not pooled.
TABPFN_API_TOKEN=
